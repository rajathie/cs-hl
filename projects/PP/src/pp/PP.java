
public class PP {

	public static void main(String[] args) {
		String password; 
		String userInput;
		boolean found = false;
		int tries = 0;
		
		
		do {
			
		} while ( tries < 3 && !found);
		// de-Morgan: !(tries > 3 || found)
	
	}
}
